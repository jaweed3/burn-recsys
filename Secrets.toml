# Configuration for the recommendation server
# These values are loaded by Shuttle.
# For local runs, create a `Secrets.toml` file with the same structure.
model = "checkpoints/best"
num_users = 10000
num_items = 7988
port = 3000
gmf_dim = 64
mlp_embed_dim = 64
