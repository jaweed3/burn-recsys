[project]
name = "burn-recsys-scripts"
version = "0.1.0"
description = "Python helper scripts for burn-recsys (data download & preprocessing)"
requires-python = ">=3.10"
dependencies = [
    "datasets>=2.20",
    "pandas>=2.0",
    "pyarrow>=14.0",
]

[tool.uv]
dev-dependencies = []
